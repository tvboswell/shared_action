name: Reusable workflow

on:
  workflow_call:
  workflow_dispatch:
jobs:
  call-another-reusable:
    #uses: octo-org/example-repo/.github/workflows/another-reusable.yml@main
    uses: tvboswell/github-actions-presentation/.github/workflows/basic-workflow.yml@main
    
